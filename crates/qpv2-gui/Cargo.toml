[package]
name = "qpv2-gui"
version = "2.0.0-rc.6"
edition = "2021"
description = "GUI for SPHINCS+ key vault with Passkey PRF / Touch ID support."
repository = "https://github.com/quantumpurse/quantum-purse-v2.git"
license = "MIT"

[[bin]]
name = "qpv2-gui"
path = "src/main.rs"

[dependencies]
qpv2-core = { path = "../qpv2-core" }
keychain = { path = "../keychain", features = ["fido2"] }
ckb-node = { path = "../ckb-node" }
trezor-connect = { path = "../trezor-connect" }
ckb-fips205-utils = { path = "../ckb-fips205-utils", features = ["signing"] }
ckb-gen-types = { version = "1.0.2", default-features = false }
ckb-jsonrpc-types = "1.0.2"
ckb-sdk = { version = "5.0.0", features = ["native-tls-vendored"] }
ckb-types = "=1.0.1"
eframe = "0.35.0"
hex = "0.4.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1.44"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
tracing-appender = "0.2"
names = { version = "0.14.0", default-features = false }
chrono = { version = "0.4", default-features = false, features = ["clock"] }

# Embeds the app icon (assets/icon/icon.ico) into the Windows .exe so it
# shows in Explorer and the taskbar. Windows-only; not fetched elsewhere.
[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"
